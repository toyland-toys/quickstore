# Runtime dependencies only. Development and test tooling lives in
# requirements-dev.txt, so production images stay small and build fast.

# --- Web framework ---
fastapi==0.110.1
uvicorn[standard]==0.25.0
jinja2==3.1.4          # server-rendered public storefront page

# --- Data model ---
pydantic>=2.6.4

# --- Database ---
motor==3.3.1           # async MongoDB driver
pymongo==4.5.0         # pinned to the version motor 3.3.1 expects

# --- Auth ---
bcrypt==4.1.3          # PIN hashing
pyjwt>=2.10.1          # session tokens

# --- Object storage (S3 / B2 / R2 / MinIO / Spaces) ---
# Only imported when STORAGE_BACKEND=s3, but kept here so one image serves both
# storage backends without a rebuild.
boto3>=1.34.129

# --- Images ---
pillow>=10.0.0
pillow-heif==0.18.0    # decode iPhone HEIC uploads

# --- SMS OTP delivery ---
# Only imported when the admin panel's otp_provider is switched to "twilio",
# but kept here so one image supports both without a rebuild.
twilio==9.11.1

# --- Config ---
python-dotenv>=1.0.1

# Removed as unused after auditing every import in the codebase: pandas, numpy,
# jq, typer, python-jose, passlib, requests-oauthlib, cryptography,
# email-validator, tzdata, python-multipart. Together they were most of the image
# size, and `jq` in particular needs a C toolchain and commonly fails to build on
# ARM and Alpine. Re-add any of them here if you introduce a use for it.
